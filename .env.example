RETREIVR_DOWNLOADS_DIR=/downloads  # Download output root inside the container; change if you remap the volume path.
RETREIVR_DATA_DIR=/data  # App data (SQLite/temp) root; change only if you use a different internal mount point.
RETREIVR_LOG_DIR=/logs  # Log directory inside the container; change if you want logs elsewhere.
RETREIVR_TOKENS_DIR=/tokens  # OAuth token storage location inside the container; change if you remap the tokens volume.
RETREIVR_HOST=0.0.0.0  # Bind address for the API; keep 0.0.0.0 in containers for external access.
RETREIVR_PORT=8000  # Internal API port; change only if you modify container port mapping.
